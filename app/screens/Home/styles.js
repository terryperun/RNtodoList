import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    // backgroundColor: 'red',
    // flex: 1,
  },
  textInput: {
    padding: 20,
  },
});
export default styles;
